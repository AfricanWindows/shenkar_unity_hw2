/// <summary>
/// Every value the UI can display. A view asks the registry for one of these ids
/// instead of holding a direct reference to a scene object.
/// </summary>
public enum CounterId
{
    Coins = 0,
    Health = 1,
    Axes = 2,
    Keys = 3
}
